import React, { ReactElement } from 'react'
import {makeStyles, createStyles, Theme} from '@material-ui/core/styles'


interface Props {
    
}

export default function Thanks({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
